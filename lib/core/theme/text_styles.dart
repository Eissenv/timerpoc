import 'package:flutter/material.dart';
import 'colors.dart';

class AppTextStyles {
  static const displayTimer =
  TextStyle(fontSize: 60, color: AppColors.text, fontWeight: FontWeight.w400);

  static const label =
  TextStyle(fontSize: 16, color: AppColors.text);
}
